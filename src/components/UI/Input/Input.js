import React from 'react';
import classes from './input.module.css';

const input = props => {
    return (
        <input placeholder="test" className={classes.Input} />
    );
}

export default input;